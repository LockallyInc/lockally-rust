/*
 * lockally control plane
 *
 * The lockally control plane lets integrators and direct customers manage everything except actual mail data flow (which uses standard JMAP / IMAP / SMTP-submission against the data plane endpoints).  **Authentication.** All `/v1/_*` endpoints require a Bearer API key in the `Authorization` header. Keys are formatted `lk_live_<8-char-prefix>_<32-char-secret>`. Generate the first key for a tenant via `cmd/seed`; subsequent keys via `POST /v1/api-keys`.  **Errors.** Failures return [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json` documents.  **Scopes.** Each endpoint requires a specific scope on the presented key. Insufficient scope returns `403` with the required scope name in `detail`. 
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@lockally.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Domain {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "tenant_id")]
    pub tenant_id: uuid::Uuid,
    #[serde(rename = "domain")]
    pub domain: String,
    #[serde(rename = "verification_token")]
    pub verification_token: String,
    #[serde(rename = "verified")]
    pub verified: bool,
    #[serde(rename = "verified_at", skip_serializing_if = "Option::is_none")]
    pub verified_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "dkim_selector")]
    pub dkim_selector: String,
    #[serde(rename = "dkim_public_record")]
    pub dkim_public_record: String,
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    /// DNS records the tenant must publish under their own DNS.
    #[serde(rename = "records", skip_serializing_if = "Option::is_none")]
    pub records: Option<Vec<models::DnsRecord>>,
}

impl Domain {
    pub fn new(id: uuid::Uuid, tenant_id: uuid::Uuid, domain: String, verification_token: String, verified: bool, dkim_selector: String, dkim_public_record: String, created_at: chrono::DateTime<chrono::FixedOffset>) -> Domain {
        Domain {
            id,
            tenant_id,
            domain,
            verification_token,
            verified,
            verified_at: None,
            dkim_selector,
            dkim_public_record,
            created_at,
            records: None,
        }
    }
}

