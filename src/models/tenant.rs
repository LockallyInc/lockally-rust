/*
 * lockally control plane
 *
 * The lockally control plane lets integrators and direct customers manage everything except actual mail data flow (which uses standard JMAP / IMAP / SMTP-submission against the data plane endpoints).  **Authentication.** All `/v1/_*` endpoints require a Bearer API key in the `Authorization` header. Keys are formatted `lk_live_<8-char-prefix>_<32-char-secret>`. Generate the first key for a tenant via `cmd/seed`; subsequent keys via `POST /v1/api-keys`.  **Errors.** Failures return [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json` documents.  **Scopes.** Each endpoint requires a specific scope on the presented key. Insufficient scope returns `403` with the required scope name in `detail`. 
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@lockally.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Tenant {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "slug")]
    pub slug: String,
    #[serde(rename = "display_name")]
    pub display_name: String,
    #[serde(rename = "status")]
    pub status: Status,
    #[serde(rename = "plan")]
    pub plan: String,
    /// Per-tenant share of the per-VPS 5/min outbound cap (L6).
    #[serde(rename = "rate_cap_per_min")]
    pub rate_cap_per_min: i32,
    #[serde(rename = "daily_msg_quota")]
    pub daily_msg_quota: i32,
    #[serde(rename = "admin_email")]
    pub admin_email: String,
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "suspended_at", skip_serializing_if = "Option::is_none")]
    pub suspended_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "closed_at", skip_serializing_if = "Option::is_none")]
    pub closed_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "hard_delete_after", skip_serializing_if = "Option::is_none")]
    pub hard_delete_after: Option<chrono::DateTime<chrono::FixedOffset>>,
}

impl Tenant {
    pub fn new(id: uuid::Uuid, slug: String, display_name: String, status: Status, plan: String, rate_cap_per_min: i32, daily_msg_quota: i32, admin_email: String, created_at: chrono::DateTime<chrono::FixedOffset>) -> Tenant {
        Tenant {
            id,
            slug,
            display_name,
            status,
            plan,
            rate_cap_per_min,
            daily_msg_quota,
            admin_email,
            created_at,
            suspended_at: None,
            closed_at: None,
            hard_delete_after: None,
        }
    }
}
/// 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Status {
    #[serde(rename = "active")]
    Active,
    #[serde(rename = "suspended")]
    Suspended,
    #[serde(rename = "closing")]
    Closing,
    #[serde(rename = "closed")]
    Closed,
}

impl Default for Status {
    fn default() -> Status {
        Self::Active
    }
}

